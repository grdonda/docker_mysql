# Comando para a criação da base de dados default

# SET character_set_client = utf8;
# SET character_set_connection = utf8;
# SET character_set_results = utf8;
# SET collation_connection = utf8_general_ci;

# comando para remover o sudo do root

# comando para criação de uma base de dados
CREATE database IF NOT EXISTS docker_mysql_001 collate utf8_general_ci character set utf8;
CREATE database IF NOT EXISTS alura_vollmed collate utf8_general_ci character set utf8;